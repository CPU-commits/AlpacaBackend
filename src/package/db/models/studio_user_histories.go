// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// StudioUserHistory is an object representing the database table.
type StudioUserHistory struct {
	ID        int64     `boil:"id" json:"id" toml:"id" yaml:"id"`
	IDUser    int64     `boil:"id_user" json:"id_user" toml:"id_user" yaml:"id_user"`
	IDStudio  int64     `boil:"id_studio" json:"id_studio" toml:"id_studio" yaml:"id_studio"`
	RemovedAt null.Time `boil:"removed_at" json:"removed_at,omitempty" toml:"removed_at" yaml:"removed_at,omitempty"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *studioUserHistoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L studioUserHistoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var StudioUserHistoryColumns = struct {
	ID        string
	IDUser    string
	IDStudio  string
	RemovedAt string
	CreatedAt string
}{
	ID:        "id",
	IDUser:    "id_user",
	IDStudio:  "id_studio",
	RemovedAt: "removed_at",
	CreatedAt: "created_at",
}

var StudioUserHistoryTableColumns = struct {
	ID        string
	IDUser    string
	IDStudio  string
	RemovedAt string
	CreatedAt string
}{
	ID:        "studio_user_histories.id",
	IDUser:    "studio_user_histories.id_user",
	IDStudio:  "studio_user_histories.id_studio",
	RemovedAt: "studio_user_histories.removed_at",
	CreatedAt: "studio_user_histories.created_at",
}

// Generated where

var StudioUserHistoryWhere = struct {
	ID        whereHelperint64
	IDUser    whereHelperint64
	IDStudio  whereHelperint64
	RemovedAt whereHelpernull_Time
	CreatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "\"studio_user_histories\".\"id\""},
	IDUser:    whereHelperint64{field: "\"studio_user_histories\".\"id_user\""},
	IDStudio:  whereHelperint64{field: "\"studio_user_histories\".\"id_studio\""},
	RemovedAt: whereHelpernull_Time{field: "\"studio_user_histories\".\"removed_at\""},
	CreatedAt: whereHelpertime_Time{field: "\"studio_user_histories\".\"created_at\""},
}

// StudioUserHistoryRels is where relationship names are stored.
var StudioUserHistoryRels = struct {
	IDStudioStudio string
	IDUserUser     string
}{
	IDStudioStudio: "IDStudioStudio",
	IDUserUser:     "IDUserUser",
}

// studioUserHistoryR is where relationships are stored.
type studioUserHistoryR struct {
	IDStudioStudio *Studio `boil:"IDStudioStudio" json:"IDStudioStudio" toml:"IDStudioStudio" yaml:"IDStudioStudio"`
	IDUserUser     *User   `boil:"IDUserUser" json:"IDUserUser" toml:"IDUserUser" yaml:"IDUserUser"`
}

// NewStruct creates a new relationship struct
func (*studioUserHistoryR) NewStruct() *studioUserHistoryR {
	return &studioUserHistoryR{}
}

func (o *StudioUserHistory) GetIDStudioStudio() *Studio {
	if o == nil {
		return nil
	}

	return o.R.GetIDStudioStudio()
}

func (r *studioUserHistoryR) GetIDStudioStudio() *Studio {
	if r == nil {
		return nil
	}

	return r.IDStudioStudio
}

func (o *StudioUserHistory) GetIDUserUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetIDUserUser()
}

func (r *studioUserHistoryR) GetIDUserUser() *User {
	if r == nil {
		return nil
	}

	return r.IDUserUser
}

// studioUserHistoryL is where Load methods for each relationship are stored.
type studioUserHistoryL struct{}

var (
	studioUserHistoryAllColumns            = []string{"id", "id_user", "id_studio", "removed_at", "created_at"}
	studioUserHistoryColumnsWithoutDefault = []string{"id_user", "id_studio"}
	studioUserHistoryColumnsWithDefault    = []string{"id", "removed_at", "created_at"}
	studioUserHistoryPrimaryKeyColumns     = []string{"id"}
	studioUserHistoryGeneratedColumns      = []string{}
)

type (
	// StudioUserHistorySlice is an alias for a slice of pointers to StudioUserHistory.
	// This should almost always be used instead of []StudioUserHistory.
	StudioUserHistorySlice []*StudioUserHistory
	// StudioUserHistoryHook is the signature for custom StudioUserHistory hook methods
	StudioUserHistoryHook func(context.Context, boil.ContextExecutor, *StudioUserHistory) error

	studioUserHistoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	studioUserHistoryType                 = reflect.TypeOf(&StudioUserHistory{})
	studioUserHistoryMapping              = queries.MakeStructMapping(studioUserHistoryType)
	studioUserHistoryPrimaryKeyMapping, _ = queries.BindMapping(studioUserHistoryType, studioUserHistoryMapping, studioUserHistoryPrimaryKeyColumns)
	studioUserHistoryInsertCacheMut       sync.RWMutex
	studioUserHistoryInsertCache          = make(map[string]insertCache)
	studioUserHistoryUpdateCacheMut       sync.RWMutex
	studioUserHistoryUpdateCache          = make(map[string]updateCache)
	studioUserHistoryUpsertCacheMut       sync.RWMutex
	studioUserHistoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var studioUserHistoryAfterSelectMu sync.Mutex
var studioUserHistoryAfterSelectHooks []StudioUserHistoryHook

var studioUserHistoryBeforeInsertMu sync.Mutex
var studioUserHistoryBeforeInsertHooks []StudioUserHistoryHook
var studioUserHistoryAfterInsertMu sync.Mutex
var studioUserHistoryAfterInsertHooks []StudioUserHistoryHook

var studioUserHistoryBeforeUpdateMu sync.Mutex
var studioUserHistoryBeforeUpdateHooks []StudioUserHistoryHook
var studioUserHistoryAfterUpdateMu sync.Mutex
var studioUserHistoryAfterUpdateHooks []StudioUserHistoryHook

var studioUserHistoryBeforeDeleteMu sync.Mutex
var studioUserHistoryBeforeDeleteHooks []StudioUserHistoryHook
var studioUserHistoryAfterDeleteMu sync.Mutex
var studioUserHistoryAfterDeleteHooks []StudioUserHistoryHook

var studioUserHistoryBeforeUpsertMu sync.Mutex
var studioUserHistoryBeforeUpsertHooks []StudioUserHistoryHook
var studioUserHistoryAfterUpsertMu sync.Mutex
var studioUserHistoryAfterUpsertHooks []StudioUserHistoryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *StudioUserHistory) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range studioUserHistoryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *StudioUserHistory) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range studioUserHistoryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *StudioUserHistory) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range studioUserHistoryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *StudioUserHistory) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range studioUserHistoryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *StudioUserHistory) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range studioUserHistoryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *StudioUserHistory) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range studioUserHistoryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *StudioUserHistory) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range studioUserHistoryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *StudioUserHistory) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range studioUserHistoryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *StudioUserHistory) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range studioUserHistoryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddStudioUserHistoryHook registers your hook function for all future operations.
func AddStudioUserHistoryHook(hookPoint boil.HookPoint, studioUserHistoryHook StudioUserHistoryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		studioUserHistoryAfterSelectMu.Lock()
		studioUserHistoryAfterSelectHooks = append(studioUserHistoryAfterSelectHooks, studioUserHistoryHook)
		studioUserHistoryAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		studioUserHistoryBeforeInsertMu.Lock()
		studioUserHistoryBeforeInsertHooks = append(studioUserHistoryBeforeInsertHooks, studioUserHistoryHook)
		studioUserHistoryBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		studioUserHistoryAfterInsertMu.Lock()
		studioUserHistoryAfterInsertHooks = append(studioUserHistoryAfterInsertHooks, studioUserHistoryHook)
		studioUserHistoryAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		studioUserHistoryBeforeUpdateMu.Lock()
		studioUserHistoryBeforeUpdateHooks = append(studioUserHistoryBeforeUpdateHooks, studioUserHistoryHook)
		studioUserHistoryBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		studioUserHistoryAfterUpdateMu.Lock()
		studioUserHistoryAfterUpdateHooks = append(studioUserHistoryAfterUpdateHooks, studioUserHistoryHook)
		studioUserHistoryAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		studioUserHistoryBeforeDeleteMu.Lock()
		studioUserHistoryBeforeDeleteHooks = append(studioUserHistoryBeforeDeleteHooks, studioUserHistoryHook)
		studioUserHistoryBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		studioUserHistoryAfterDeleteMu.Lock()
		studioUserHistoryAfterDeleteHooks = append(studioUserHistoryAfterDeleteHooks, studioUserHistoryHook)
		studioUserHistoryAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		studioUserHistoryBeforeUpsertMu.Lock()
		studioUserHistoryBeforeUpsertHooks = append(studioUserHistoryBeforeUpsertHooks, studioUserHistoryHook)
		studioUserHistoryBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		studioUserHistoryAfterUpsertMu.Lock()
		studioUserHistoryAfterUpsertHooks = append(studioUserHistoryAfterUpsertHooks, studioUserHistoryHook)
		studioUserHistoryAfterUpsertMu.Unlock()
	}
}

// One returns a single studioUserHistory record from the query.
func (q studioUserHistoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*StudioUserHistory, error) {
	o := &StudioUserHistory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for studio_user_histories")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all StudioUserHistory records from the query.
func (q studioUserHistoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (StudioUserHistorySlice, error) {
	var o []*StudioUserHistory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to StudioUserHistory slice")
	}

	if len(studioUserHistoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all StudioUserHistory records in the query.
func (q studioUserHistoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count studio_user_histories rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q studioUserHistoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if studio_user_histories exists")
	}

	return count > 0, nil
}

// IDStudioStudio pointed to by the foreign key.
func (o *StudioUserHistory) IDStudioStudio(mods ...qm.QueryMod) studioQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.IDStudio),
	}

	queryMods = append(queryMods, mods...)

	return Studios(queryMods...)
}

// IDUserUser pointed to by the foreign key.
func (o *StudioUserHistory) IDUserUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.IDUser),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadIDStudioStudio allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (studioUserHistoryL) LoadIDStudioStudio(ctx context.Context, e boil.ContextExecutor, singular bool, maybeStudioUserHistory interface{}, mods queries.Applicator) error {
	var slice []*StudioUserHistory
	var object *StudioUserHistory

	if singular {
		var ok bool
		object, ok = maybeStudioUserHistory.(*StudioUserHistory)
		if !ok {
			object = new(StudioUserHistory)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeStudioUserHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeStudioUserHistory))
			}
		}
	} else {
		s, ok := maybeStudioUserHistory.(*[]*StudioUserHistory)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeStudioUserHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeStudioUserHistory))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &studioUserHistoryR{}
		}
		args[object.IDStudio] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &studioUserHistoryR{}
			}

			args[obj.IDStudio] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`studios`),
		qm.WhereIn(`studios.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Studio")
	}

	var resultSlice []*Studio
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Studio")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for studios")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for studios")
	}

	if len(studioAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.IDStudioStudio = foreign
		if foreign.R == nil {
			foreign.R = &studioR{}
		}
		foreign.R.IDStudioStudioUserHistories = append(foreign.R.IDStudioStudioUserHistories, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.IDStudio == foreign.ID {
				local.R.IDStudioStudio = foreign
				if foreign.R == nil {
					foreign.R = &studioR{}
				}
				foreign.R.IDStudioStudioUserHistories = append(foreign.R.IDStudioStudioUserHistories, local)
				break
			}
		}
	}

	return nil
}

// LoadIDUserUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (studioUserHistoryL) LoadIDUserUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeStudioUserHistory interface{}, mods queries.Applicator) error {
	var slice []*StudioUserHistory
	var object *StudioUserHistory

	if singular {
		var ok bool
		object, ok = maybeStudioUserHistory.(*StudioUserHistory)
		if !ok {
			object = new(StudioUserHistory)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeStudioUserHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeStudioUserHistory))
			}
		}
	} else {
		s, ok := maybeStudioUserHistory.(*[]*StudioUserHistory)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeStudioUserHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeStudioUserHistory))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &studioUserHistoryR{}
		}
		args[object.IDUser] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &studioUserHistoryR{}
			}

			args[obj.IDUser] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.IDUserUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.IDUserStudioUserHistories = append(foreign.R.IDUserStudioUserHistories, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.IDUser == foreign.ID {
				local.R.IDUserUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.IDUserStudioUserHistories = append(foreign.R.IDUserStudioUserHistories, local)
				break
			}
		}
	}

	return nil
}

// SetIDStudioStudio of the studioUserHistory to the related item.
// Sets o.R.IDStudioStudio to related.
// Adds o to related.R.IDStudioStudioUserHistories.
func (o *StudioUserHistory) SetIDStudioStudio(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Studio) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"studio_user_histories\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"id_studio"}),
		strmangle.WhereClause("\"", "\"", 2, studioUserHistoryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.IDStudio = related.ID
	if o.R == nil {
		o.R = &studioUserHistoryR{
			IDStudioStudio: related,
		}
	} else {
		o.R.IDStudioStudio = related
	}

	if related.R == nil {
		related.R = &studioR{
			IDStudioStudioUserHistories: StudioUserHistorySlice{o},
		}
	} else {
		related.R.IDStudioStudioUserHistories = append(related.R.IDStudioStudioUserHistories, o)
	}

	return nil
}

// SetIDUserUser of the studioUserHistory to the related item.
// Sets o.R.IDUserUser to related.
// Adds o to related.R.IDUserStudioUserHistories.
func (o *StudioUserHistory) SetIDUserUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"studio_user_histories\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"id_user"}),
		strmangle.WhereClause("\"", "\"", 2, studioUserHistoryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.IDUser = related.ID
	if o.R == nil {
		o.R = &studioUserHistoryR{
			IDUserUser: related,
		}
	} else {
		o.R.IDUserUser = related
	}

	if related.R == nil {
		related.R = &userR{
			IDUserStudioUserHistories: StudioUserHistorySlice{o},
		}
	} else {
		related.R.IDUserStudioUserHistories = append(related.R.IDUserStudioUserHistories, o)
	}

	return nil
}

// StudioUserHistories retrieves all the records using an executor.
func StudioUserHistories(mods ...qm.QueryMod) studioUserHistoryQuery {
	mods = append(mods, qm.From("\"studio_user_histories\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"studio_user_histories\".*"})
	}

	return studioUserHistoryQuery{q}
}

// FindStudioUserHistory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindStudioUserHistory(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*StudioUserHistory, error) {
	studioUserHistoryObj := &StudioUserHistory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"studio_user_histories\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, studioUserHistoryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from studio_user_histories")
	}

	if err = studioUserHistoryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return studioUserHistoryObj, err
	}

	return studioUserHistoryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *StudioUserHistory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no studio_user_histories provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(studioUserHistoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	studioUserHistoryInsertCacheMut.RLock()
	cache, cached := studioUserHistoryInsertCache[key]
	studioUserHistoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			studioUserHistoryAllColumns,
			studioUserHistoryColumnsWithDefault,
			studioUserHistoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(studioUserHistoryType, studioUserHistoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(studioUserHistoryType, studioUserHistoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"studio_user_histories\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"studio_user_histories\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into studio_user_histories")
	}

	if !cached {
		studioUserHistoryInsertCacheMut.Lock()
		studioUserHistoryInsertCache[key] = cache
		studioUserHistoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the StudioUserHistory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *StudioUserHistory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	studioUserHistoryUpdateCacheMut.RLock()
	cache, cached := studioUserHistoryUpdateCache[key]
	studioUserHistoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			studioUserHistoryAllColumns,
			studioUserHistoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update studio_user_histories, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"studio_user_histories\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, studioUserHistoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(studioUserHistoryType, studioUserHistoryMapping, append(wl, studioUserHistoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update studio_user_histories row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for studio_user_histories")
	}

	if !cached {
		studioUserHistoryUpdateCacheMut.Lock()
		studioUserHistoryUpdateCache[key] = cache
		studioUserHistoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q studioUserHistoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for studio_user_histories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for studio_user_histories")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o StudioUserHistorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), studioUserHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"studio_user_histories\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, studioUserHistoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in studioUserHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all studioUserHistory")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *StudioUserHistory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no studio_user_histories provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(studioUserHistoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	studioUserHistoryUpsertCacheMut.RLock()
	cache, cached := studioUserHistoryUpsertCache[key]
	studioUserHistoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			studioUserHistoryAllColumns,
			studioUserHistoryColumnsWithDefault,
			studioUserHistoryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			studioUserHistoryAllColumns,
			studioUserHistoryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert studio_user_histories, could not build update column list")
		}

		ret := strmangle.SetComplement(studioUserHistoryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(studioUserHistoryPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert studio_user_histories, could not build conflict column list")
			}

			conflict = make([]string, len(studioUserHistoryPrimaryKeyColumns))
			copy(conflict, studioUserHistoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"studio_user_histories\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(studioUserHistoryType, studioUserHistoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(studioUserHistoryType, studioUserHistoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert studio_user_histories")
	}

	if !cached {
		studioUserHistoryUpsertCacheMut.Lock()
		studioUserHistoryUpsertCache[key] = cache
		studioUserHistoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single StudioUserHistory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *StudioUserHistory) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no StudioUserHistory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), studioUserHistoryPrimaryKeyMapping)
	sql := "DELETE FROM \"studio_user_histories\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from studio_user_histories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for studio_user_histories")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q studioUserHistoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no studioUserHistoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from studio_user_histories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for studio_user_histories")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o StudioUserHistorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(studioUserHistoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), studioUserHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"studio_user_histories\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, studioUserHistoryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from studioUserHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for studio_user_histories")
	}

	if len(studioUserHistoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *StudioUserHistory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindStudioUserHistory(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *StudioUserHistorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := StudioUserHistorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), studioUserHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"studio_user_histories\".* FROM \"studio_user_histories\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, studioUserHistoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in StudioUserHistorySlice")
	}

	*o = slice

	return nil
}

// StudioUserHistoryExists checks if the StudioUserHistory row exists.
func StudioUserHistoryExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"studio_user_histories\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if studio_user_histories exists")
	}

	return exists, nil
}

// Exists checks if the StudioUserHistory row exists.
func (o *StudioUserHistory) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return StudioUserHistoryExists(ctx, exec, o.ID)
}
